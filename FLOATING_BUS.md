# Floating Bus(浮空匯流排)筆記

> 為什麼讀一個「沒人驅動」的位址,會拿到「畫面的位元組」?而遊戲又怎麼拿它當亂數?
> 這份筆記記錄這次為 apple2emu 補上 floating bus 的來龍去脈,之後慢慢理解用。

---

## 0. 一句話總結

**floating bus 不是一條特殊的線,而是「同一條資料匯流排在沒人驅動時,殘留著影像電路上半 cycle 剛留下的畫面位元組」這個電路副作用。** 德軍總部(Castle Wolfenstein)就是白嫖這個副作用來當亂數,用來產生爆炸白雜訊,以及防彈背心的中彈判定。

---

## 1. 觸發這次調查的兩個現象

在《德軍總部》中:

1. **爆炸聲變單音**:原本是「沙——」的白雜訊爆炸聲,變成固定頻率的「嗶——」。
2. **防彈背心失效**:原本穿背心(玩家或 NPC)要開槍多次才會傷到,變成一發就傷到。

兩個看似無關的系統(音效 / 戰鬥判定)同時壞掉,共通點是**都依賴同一個亂數源**。這比兩個獨立 bug 合理得多 → 指向「亂數來源壞掉」。

---

## 2. Apple II 沒有亂數硬體,亂數從哪來?

Apple II 沒有 RNG 晶片。遊戲最常見的硬體亂數來源就是 **floating bus**:去讀 `$C0xx` 區段裡那些「沒有任何晶片驅動資料匯流排」的位址,拿到的殘留值,其實是**影像電路此刻正在抓取的那個顯示位元組**。

---

## 3. 為什麼「沒人驅動的位址」會回傳「畫面位元組」?(核心)

關鍵在三件事:**共用的資料匯流排**、**記憶體一個 cycle 被讀兩次**、**匯流排是會殘留電荷的裸線**。

### 3-1. 整台機器只有一條資料匯流排

6502、RAM、ROM、I/O、影像電路全部掛在**同一組 8 條資料線**上。任何一次讀取:

```
位址送出 → 位址解碼 → 對應晶片被選中(chip select)→ 該晶片「主動把值推上匯流排」→ CPU 鎖存
```

同一時間只能有一個晶片在驅動匯流排,否則會打架。

### 3-2. Apple II 的 RAM,一個 cycle 被讀兩次

Woz 的精巧設計:Apple II **沒有獨立顯示記憶體**,畫面和程式共用同一塊 RAM。為了不打架,RAM 在每個 ~1MHz cycle 裡被存取兩次,分上下半:

```
一個 CPU cycle(約 1µs):
┌──────────────┬──────────────┐
│   前半        │   後半        │
│  影像電路讀    │   CPU 讀      │
│ RAM 抓畫面byte│  (程式的存取) │
└──────────────┴──────────────┘
```

→ **每個 cycle 前半,影像電路一定去 RAM 抓一個畫面位元組並推上匯流排。** 影像電路「無時無刻」都在驅動匯流排。

### 3-3. 讀到沒人驅動的位址時

CPU 在後半讀 `$C030`(喇叭開關)。`$C030` **接受讀取動作(觸發 toggle),但它沒有資料要回**,不驅動資料線;RAM/ROM 也沒被選中。

→ **後半這個瞬間,沒有任何晶片在驅動匯流排。**

但匯流排是**實體銅線,有寄生電容**。沒人驅動時不會瞬間歸零,而是**像小電容一樣保持「上一次被驅動的電壓」**(這就是「floating / 浮空」)。

那「上一次被驅動」是什麼時候?就是**同一個 cycle 前半,影像電路剛抓的那個畫面位元組**:

```
前半:影像電路把畫面byte推上匯流排  →  匯流排 = 畫面byte
後半:CPU 讀 $C030,沒人驅動        →  匯流排殘留 = 還是那個畫面byte
                                   →  CPU 鎖存到的就是那個畫面byte
```

**所以 CPU 讀到的 floating bus 值 = 影像電路剛抓的那個畫面位元組。** 不是設計出來的功能,是「沒人覆蓋、殘留電荷剛好是它」的物理結果。

### 3-4. 為什麼對程式來說是「亂數」

- 影像電路跟著映像管掃描在跑,它**此刻掃到畫面記憶體哪一格**,跟程式跑到哪一行**完全不同步**。
- 程式無法預測自己執行 `LDA $C030` 那一瞬間影像電路掃到哪 → 抓回的位元組不可預測。
- 畫面記憶體裝的是遊戲圖形,本身內容五花八門 → 值的分布夠雜。

兩者疊加 → 對程式而言就是免費、夠亂的亂數源。

⚠️ 注意:floating bus **不是真亂數**,它是有確定內容的(畫面 RAM 那一格)。所以模擬時不能隨便回一個浮動值,必須回**正確的那一格**——否則有些靠 floating bus 偵測垂直消隱(VBL)的 II+ 程式(II+ 沒有 VBL 中斷)會壞掉。

---

## 4. 德軍總部那邊怎麼用(程式視角)

(以下是 Apple II 產生白雜訊的標準手法,未反組譯確切位址,但走的就是這條路)

爆炸聲迴圈概念上:

```asm
loop:
    LDA  $C030      ; ① 推一下喇叭(toggle),這個讀取「同時」回傳 floating bus 到 A
    AND  #$xx       ; ② 取亂數的幾個 bit,當成這次要等多久
    TAX
delay:
    DEX             ; ③ 倒數延遲(長度由亂數決定)
    BNE  delay
    DEC  counter    ; ④ 還沒爆完就繼續
    BNE  loop
```

關鍵在 ①:`LDA $C030` 做了兩件事——
- **副作用**:把喇叭 toggle 一次
- **回傳值**:`$C030` 沒晶片驅動,CPU 讀到 floating bus(影像電路此刻抓的畫面位元組)

→ 延遲每圈不一樣 → toggle 節奏忽快忽慢 → **白雜訊 → 爆炸聲**。
規律 toggle = 單音;不規律 toggle = 白雜訊。爆炸聲的本質就是「用亂數打亂 toggle 間隔」。

防彈背心同理:中彈是否被擋下是一次「擲骰」(取 floating bus 亂數和門檻比較)。亂數恆定 → 永遠落同一邊 → 永遠穿透 → 一發就傷到。

---

## 5. 模擬器這邊改了什麼(`apple2-core/src/memory.rs`)

問題:模擬器忠實做了「toggle 喇叭」的副作用,卻**沒有忠實回傳 floating bus 的值**,而是寫死 `0`。

**改之前:**
```rust
0xC030..=0xC03F => {
    self.toggle_speaker(access_cycle);
    0                       // ← 回傳值寫死 0,亂數永遠 0 → 單音
}
```

**改之後:**
```rust
0xC030..=0xC03F => {
    self.toggle_speaker(access_cycle);          // 副作用照舊
    self.floating_bus(access_cycle)             // ← 回傳真正的 floating bus
}
```

`floating_bus()` 做的事 = 用程式重現那個電路結果(模擬器沒有銅線/電容/殘留電荷):

```
floating_bus(cycle):
   1. video_scanner_address(cycle)   // 用「現在第幾個 CPU cycle」反推影像電路此刻指向哪一格 RAM
                                      //   (65 clocks/掃描線 × 262 線的 NTSC 時序;Sather 位址公式)
   2. 回傳 self.ram[那個位址]          // 回傳那一格的內容
```

`video_scanner_address()` 是移植自 **AppleWin 的 `VideoGetScannerAddress`**,實作 Jim Sather《Understanding the Apple IIe》第 5 章的視訊掃描器位址產生模型(H/V counter → 位址的位元組合,含 HBL/VBL 期間的位址)。

### 涵蓋範圍
回傳值統一用 floating bus 的,是所有「未驅動」的 `$C0xx`:
- `$C050`–`$C057`(影像軟開關)
- `$C030`–`$C03F`(喇叭)
- `$C080`–`$C08F`(語言卡開關)
- 萬用 fallback `_ =>`(空插槽 ROM 空間等)

**所有原本的副作用都保留**(切模式、toggle 喇叭、LC bank 切換),只把回傳值從 `0` 改成 floating bus。

### 重要觀念釐清
- **回傳值**:可以、也應該用一支統一的 `floating_bus()` 處理,不必逐位址對應。✔(現狀)
- **但 floating bus 值本身必須算掃描器位址**(它是特定畫面 RAM 那一格,不是隨機數),`video_scanner_address()` 省不掉。
- **副作用必須逐位址解碼**,不能用 floating 取代($C050 切模式、$C030 toggle、$C0Ex 驅動磁碟……都是遊戲依賴的)。
- 記憶體圖中,只有 `$C000–$CFFF` I/O 區會 floating;`$0000–$BFFF` 全 RAM、`$D000–$FFFF` 全 ROM/LC,都有驅動,沒有未定義空間。

---

## 6. 驗證

- `cargo test -p apple2-core`:**50/50 通過**(無回歸)。
- 新增測試 `undriven_io_reads_track_the_floating_bus`:把 RAM 填上位址低位元組標記,連讀 `$C055` 200 次,確認回傳值**確實隨 cycle 變動且取自 RAM**(舊的常數 stub 會讓此測試失敗)。
- 實機視窗:德軍總部爆炸聲從單音恢復成白雜訊爆炸聲。✔

---

## 7. 參考

- AppleWin `Video.cpp` → `VideoGetScannerAddress`(本次位址公式來源)
- Jim Sather, *Understanding the Apple IIe*, 第 5 章(視訊掃描器時序與位址產生)
